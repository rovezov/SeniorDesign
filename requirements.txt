# Python Package Requirements
# Target runtime: Python 3.12.3
#
# !! DO NOT run bare: pip install -r requirements.txt !!
# insightface pulls in albumentations, which installs opencv-python-headless and
# overwrites the GUI-capable OpenCV, breaking cv2.imshow().
#
# Use the provided install script instead:
#   Windows : .\install.ps1
#   Linux   : bash install.sh
#
# Or manually:
#   pip install -r requirements.txt
#   pip install --force-reinstall opencv-contrib-python==4.13.0.92

# Core Dependencies
# Pin numpy to 1.x for opencv 4.8.1.78 and mediapipe compatibility
numpy<2,>=1.21
onnxruntime>=1.12.0
scipy>=1.7.0

# Face Detection/Recognition
# 0.10.9 does not provide Python 3.12 wheels; pin to a 3.12-compatible build.
cvzone>=1.5.0
# insightface: provides RetinaFace detector for face_detector.py + edge-optimized ArcFace (buffalo_sc/MobileFaceNet)
# Windows requires Microsoft C++ Build Tools: https://visualstudio.microsoft.com/visual-cpp-build-tools/
insightface>=0.7.3

# Protobuf (required for mediapipe compatibility)
protobuf<5,>=4.25.3

# Computer Vision — listed last so pip resolves it after insightface/albumentations.
# The force-reinstall in install.ps1 / install.sh ensures headless is removed.
# Using 4.8.1.78 (compatible with numpy<2) instead of 4.13.0.92 (requires numpy>=2)
# opencv-contrib-python==4.8.1.78